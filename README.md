# TaskFlow

## Descrição
Gerenciador de tarefas colaborativo com autenticação, CRUD e dashboard.

### Tecnologias
- Backend: Node.js, Express, MongoDB, JWT
- Frontend: React, Vite, TailwindCSS

### Como rodar
1. Instale dependências em backend e frontend.
2. Configure .env com MONGO_URI e JWT_SECRET.
3. Execute `npm start` em ambos.

📦 O que está incluso:

Backend (Node.js + Express + MongoDB)

server.js com configuração do servidor e rotas.
Modelos Task.js e User.js.


Frontend (React + Vite + TailwindCSS)

Estrutura com páginas: Login, Dashboard, Tasks.
App.jsx com rotas configuradas.


README.md com instruções para rodar o projeto.

🚀 Como rodar:

Backend

cd backend
npm install
# Configure .env com MONGO_URI e JWT_SECRET

Frontend

cd frontend
npm install
npm run dev
